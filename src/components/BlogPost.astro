---
const { title, publishDate, authorName, authorSocial } = Astro.props;
---
<article class="article">
		<header>
			<h1>{title}</h1>
			<p>By : <a href={authorSocial}>{authorName}</a><br/>{publishDate}</p>
		</header>
		<main>
			<slot />
		</main>
</article>
