<style lang="css" is:inline>
	html {
		background: var(--color-black);
		color: var(--color-grey);
		font-family: sans-serif;
		font-style: normal;
		font-weight: 400;
	}

	body {
		margin: 0px;
	}

	main {
		min-height: 1200px;
	}
	div, p {
		line-height: 1.8rem;
	}

</style>